From cb6e482e77b9efae719adb255521025f92fc02da Mon Sep 17 00:00:00 2001
Message-Id: <cb6e482e77b9efae719adb255521025f92fc02da.1612803844.git.marksvc@gmail.com>
From: MarkS <marksvc@gmail.com>
Date: Fri, 5 Feb 2021 12:27:09 -0600
Subject: [PATCH] Refine syntax for environ overridable defaults

Change-Id: I7101e3e62d351c6ab22e43d16c986ad634c894ee
---
 environ | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/environ b/environ
index 8d6097d8..80702df2 100644
--- a/fw/environ
+++ b/fw/environ
@@ -33,9 +33,9 @@ fi
 BASE=$(pwd)
 COM=$(dirname "${BASE}")/libcom/COM
 ARCH=$(uname -m)
-[ -z "$BUILD" ] && BUILD=Debug
-[ -z "$MONO_PREFIX" ] && MONO_PREFIX=/opt/mono5-sil
-[ -z "$MONO_SILPKGDIR" ] && MONO_SILPKGDIR=/opt/mono5-sil
+BUILD="${BUILD:-Debug}"
+MONO_PREFIX="${MONO_PREFIX:-/opt/mono5-sil}"
+MONO_SILPKGDIR="${MONO_SILPKGDIR:-/opt/mono5-sil}"
 
 # Dependency locations
 # search for xulrunner and geckofx, select the best, and add its location to LD_LIBRARY_PATH
@@ -60,7 +60,7 @@ ${COM}/build${ARCH}/lib:\
 ${LD_LIBRARY_PATH}"
 fi
 # ensure we scan the default pkg-config directories (to pick up Geckofx for compiling)
-[ -z "$PKG_CONFIG_PATH" ] && PKG_CONFIG_PATH=/usr/lib/pkgconfig:/usr/share/pkgconfig
+PKG_CONFIG_PATH="${PKG_CONFIG_PATH:-/usr/lib/pkgconfig:/usr/share/pkgconfig}"
 
 # Add packaged mono items to paths
 PATH="${MONO_SILPKGDIR}/bin:${PATH}"
-- 
2.25.1

